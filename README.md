# CWPP

This is an experiment that consists on a script that base on a given county and state uses google gemini API to search if there is any county wide CWPP.

> Remember to add the API key to the system in order to the scripts to work properly